# Скопируй в .env.local и при необходимости переопредели.
# Если оставить пустым — фронт ходит на тот же origin (полезно для vite proxy в dev).
# Для прода — укажи полный URL backend-сервера.
VITE_API_BASE=http://localhost:8080
